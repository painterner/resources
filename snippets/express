// import * as express from "express";

const express = require("express")

const app = express();

app.use(express.json());
app.use(express.urlencoded({extended: true}))

app.get('/vocaloid/consellation', (req, res) => {

})

app.listen(12300, () => {
    console.log('app listen at', 12300)
})
